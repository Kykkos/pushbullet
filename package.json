{
  "name": "pushbullet-websocket",
  "version": "1.0.0",
  "description": "WebSocket listener for Pushbullet SMS notifications",
  "engines": {
    "node": ">=18.11",
    "pnpm": ">=7.27"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outdir=dist --platform=node",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.13.0",
    "axios": "^1.7.2",
    "esbuild": "^0.17.16"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "@types/ws": "^8.5.4",
    "@types/axios": "^0.14.0",
    "typescript": "^4.9.5"
  }
}
